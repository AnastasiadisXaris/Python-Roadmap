# 🐍 Python Basics - Τα Πρώτα Σου Βήματα!

Η Python είναι μια από τις πιο δημοφιλείς γλώσσες προγραμματισμού. Είναι ευέλικτη, δυναμική και χρησιμοποιείται σε πολλούς τομείς, όπως ανάπτυξη ιστοσελίδων, ανάλυση δεδομένων, επιστήμη δεδομένων, τεχνητή νοημοσύνη, αυτοματισμούς και πολλά άλλα! 🚀

Η απλή σύνταξή της την καθιστά ιδανική για αρχάριους, ενώ η ισχυρή βιβλιοθήκη της την κάνει ένα πολύτιμο εργαλείο για έμπειρους προγραμματιστές.

---

## 🔹 Τι θα μάθεις;

Σε αυτόν τον οδηγό, θα μάθεις τα βασικά της Python και θα αποκτήσεις μια καλή κατανόηση των θεμελιωδών εννοιών της γλώσσας.

🔸 **Σύνταξη & μεταβλητές** - Πώς να γράφεις κώδικα Python και να χρησιμοποιείς μεταβλητές.

🔸 **Τελεστές** - Αριθμητικοί, συγκριτικοί και λογικοί τελεστές.

🔸 **Strings** - Μορφοποίηση κειμένου και χρήσιμες μέθοδοι.

🔸 **Δομές ελέγχου** - Χρήση `if`, `for`, `while` για έλεγχο της ροής του προγράμματός σου.

🔸 **Συναρτήσεις** - Οργάνωση του κώδικα με επαναχρησιμοποιήσιμες συναρτήσεις.

🔸 **Διαχείριση σφαλμάτων** - `try`/`except` για κώδικα που δεν "σπάει".

🔸 **Modules & πακέτα** - Οργάνωση και επαναχρησιμοποίηση κώδικα σε πολλά αρχεία.

🔸 **Αρχεία** - Ανάγνωση και εγγραφή δεδομένων σε αρχεία.

🔸 **Λίστες & λεξικά** - Δομές δεδομένων που θα χρησιμοποιήσεις πολύ συχνά.

🔸 **Βασικές εντολές Python** - Print, input, handling errors, και άλλα.

---

## 🔹 Πώς να ξεκινήσεις;

Για να γράψεις κώδικα σε Python, ακολούθησε τα εξής βήματα:

1️⃣ **Εγκατάσταση Python** - Κατέβασε και εγκατέστησε την Python από την [επίσημη σελίδα](https://www.python.org/downloads/).

2️⃣ **Εκτέλεση κώδικα** - Μπορείς να εκτελέσεις Python scripts από το τερματικό ή να χρησιμοποιήσεις ένα IDE όπως το PyCharm ή VS Code.

3️⃣ **Δημιουργία του πρώτου προγράμματος** - Γράψε και εκτέλεσε το πρώτο σου πρόγραμμα.

## 🔹 Αν δεν θέλεις να εγκαταστήσεις κάποιο από τα παραπάνω IDE

Μπορείς εναλλακτικά να χρησιμοποιήσεις την πλατφόρμα του [Google Colab](https://colab.research.google.com/) — τρέχει Python μέσα στον browser σου, χωρίς καμία εγκατάσταση.

---

## ΠΑΡΑΔΕΙΓΜΑΤΑ ΓΙΑ ΝΑ ΞΕΚΙΝΗΣΕΙΣ

```python
print("Γεια σου, κόσμε!")
```

Αυτό θα εμφανίσει:
```
Γεια σου, κόσμε!
```

📌 **Σχόλια στον κώδικα**

Τα σχόλια δεν εκτελούνται — χρησιμεύουν για επεξηγήσεις μέσα στον κώδικα:

```python
# Αυτό είναι ένα σχόλιο μίας γραμμής
print("Κώδικας")  # Σχόλιο δίπλα σε εντολή

"""
Αυτό είναι ένα σχόλιο
πολλαπλών γραμμών (docstring)
"""
```

---

## 🔹 Μεταβλητές & Τύποι Δεδομένων

Οι μεταβλητές αποθηκεύουν δεδομένα στη μνήμη. Στην Python, δεν χρειάζεται να δηλώσεις τον τύπο της μεταβλητής, καθώς αναγνωρίζεται αυτόματα:

```python
x = 5             # Ακέραιος αριθμός (int)
name = "Maria"    # Συμβολοσειρά (str)
pi = 3.14         # Δεκαδικός αριθμός (float)
is_active = True  # Boolean τιμή (bool)
```

📌 **Σημείωση:** Χρησιμοποίησε την `type()` για να δεις τον τύπο μιας μεταβλητής:

```python
print(type(x))      # <class 'int'>
print(type(name))    # <class 'str'>
```

### Μετατροπή Τύπων (Type Casting)

Μπορείς να μετατρέψεις μια τιμή από έναν τύπο σε άλλον:

```python
age = "25"
age_number = int(age)       # str -> int
price = float("19.99")       # str -> float
text = str(100)               # int -> str

print(age_number + 5)  # 30
```

---

## 🔹 Τελεστές (Operators)

```python
# Αριθμητικοί τελεστές
print(10 + 3)   # 13  -  πρόσθεση
print(10 - 3)   # 7   -  αφαίρεση
print(10 * 3)   # 30  -  πολλαπλασιασμός
print(10 / 3)   # 3.33... - διαίρεση (float)
print(10 // 3)  # 3   -  ακέραια διαίρεση
print(10 % 3)   # 1   -  υπόλοιπο (modulo)
print(10 ** 2)  # 100 -  ύψωση σε δύναμη

# Συγκριτικοί τελεστές
print(5 == 5)   # True
print(5 != 3)   # True
print(5 > 3)    # True

# Λογικοί τελεστές
print(True and False)  # False
print(True or False)   # True
print(not True)         # False
```

---

## 🔹 Strings - Εργασία με Κείμενο

```python
name = "Κόσμε"
greeting = "Γεια σου, " + name + "!"   # Συνένωση (concatenation)
print(greeting)
```

📌 **f-strings** — ο πιο σύγχρονος και ευανάγνωστος τρόπος μορφοποίησης κειμένου:

```python
name = "Μαρία"
age = 25
print(f"Η {name} είναι {age} ετών.")
print(f"Σε 5 χρόνια θα είναι {age + 5} ετών.")
```

📌 **Χρήσιμες μέθοδοι strings:**

```python
text = "  Καλημέρα Κόσμε  "

print(text.strip())       # "Καλημέρα Κόσμε" (αφαιρεί κενά)
print(text.lower())        # μετατροπή σε πεζά
print(text.upper())        # μετατροπή σε κεφαλαία
print(text.replace("Κόσμε", "Python"))
print(text.split(" "))      # χωρισμός σε λίστα λέξεων
print(len(text))            # μήκος του string

# Slicing - "κόψιμο" κομματιού από ένα string
word = "Python"
print(word[0])      # P
print(word[0:3])     # Pyt
print(word[-1])       # n (τελευταίος χαρακτήρας)
```

---

## 🔹 Δομές Ελέγχου

Οι δομές ελέγχου βοηθούν να εκτελείς διαφορετικά μπλοκ κώδικα με βάση συνθήκες.

### If-Elif-Else

```python
age = 18
if age >= 18:
    print("Είσαι ενήλικας!")
elif age >= 13:
    print("Είσαι έφηβος!")
else:
    print("Είσαι παιδί!")
```

### Βρόχοι (Loops)

#### For Loop:
```python
for i in range(5):
    print("Αριθμός:", i)
```

#### While Loop:
```python
x = 0
while x < 3:
    print("Το x είναι:", x)
    x += 1
```

#### Break & Continue

```python
for i in range(10):
    if i == 5:
        break        # Σταματάει εντελώς τον βρόχο
    if i % 2 == 0:
        continue      # Προσπερνάει την τρέχουσα επανάληψη
    print(i)           # Τυπώνει μόνο τους περιττούς αριθμούς < 5
```

#### List Comprehensions

Ένας συνοπτικός τρόπος να δημιουργήσεις λίστες:

```python
squares = [x**2 for x in range(6)]
print(squares)   # [0, 1, 4, 9, 16, 25]

even_only = [x for x in range(10) if x % 2 == 0]
print(even_only)  # [0, 2, 4, 6, 8]
```

---

## 🔹 Συναρτήσεις

Οι συναρτήσεις κάνουν τον κώδικά σου πιο οργανωμένο και επαναχρησιμοποιήσιμο.

```python
def greet(name):
    return f"Γεια σου, {name}!"

print(greet("κόσμε"))  # Γεια σου, κόσμε!
```

📌 **Παράμετροι με προεπιλεγμένη τιμή:**

```python
def greet(name="φίλε"):
    return f"Γεια σου, {name}!"

print(greet())          # Γεια σου, φίλε!
print(greet("Νίκο"))     # Γεια σου, Νίκο!
```

📌 **`*args` και `**kwargs`** — για συναρτήσεις με άγνωστο πλήθος ορισμάτων:

```python
def add_all(*numbers):
    return sum(numbers)

print(add_all(1, 2, 3, 4))  # 10

def print_info(**details):
    for key, value in details.items():
        print(f"{key}: {value}")

print_info(name="Άννα", age=30)
```

📌 **Lambda functions** — σύντομες, ανώνυμες συναρτήσεις μίας γραμμής:

```python
square = lambda x: x ** 2
print(square(4))  # 16

# Χρήσιμο μέσα σε sorted(), map(), filter()
numbers = [5, 2, 8, 1]
print(sorted(numbers, key=lambda x: -x))  # [8, 5, 2, 1]
```

---

## 🔹 Διαχείριση Σφαλμάτων (Error Handling)

Το `try`/`except` σου επιτρέπει να "πιάνεις" σφάλματα χωρίς να σταματάει το πρόγραμμά σου:

```python
try:
    number = int(input("Δώσε έναν αριθμό: "))
    result = 10 / number
    print(f"Αποτέλεσμα: {result}")
except ValueError:
    print("Αυτό δεν είναι έγκυρος αριθμός!")
except ZeroDivisionError:
    print("Δεν μπορείς να διαιρέσεις με το μηδέν!")
finally:
    print("Η προσπάθεια ολοκληρώθηκε.")
```

---

## 🔹 Modules & Πακέτα

Μπορείς να χωρίσεις τον κώδικά σου σε πολλά αρχεία και να τον επαναχρησιμοποιήσεις με `import`:

```python
# Ενσωματωμένα modules της Python
import math
import random
from datetime import datetime

print(math.sqrt(16))          # 4.0
print(random.randint(1, 10))   # τυχαίος αριθμός από 1 έως 10
print(datetime.now())           # τρέχουσα ημερομηνία/ώρα
```

📌 **Εγκατάσταση εξωτερικών πακέτων** με το `pip`:

```sh
pip install requests
```

```python
import requests

response = requests.get("https://api.github.com")
print(response.status_code)
```

---

## 🔹 Λίστες & Λεξικά

### Λίστες (Lists)
```python
fruits = ["μήλο", "πορτοκάλι", "μπανάνα"]
print(fruits[0])  # μήλο
fruits.append("ακτινίδιο")
print(fruits)
```

### Λεξικά (Dictionaries)
```python
person = {"name": "Γιάννης", "age": 25}
print(person["name"])  # Γιάννης
person["city"] = "Αθήνα"
```

💡 Για πιο αναλυτική κάλυψη λιστών, λεξικών, πλειάδων και συνόλων, δες τον οδηγό **Δομές Δεδομένων**.

---

## 🔹 Ανάγνωση & Εγγραφή Αρχείων

```python
# Δημιουργία και εγγραφή σε αρχείο
with open("example.txt", "w") as file:
    file.write("Hello, Python!")

# Ανάγνωση αρχείου
with open("example.txt", "r") as file:
    print(file.read())

# Προσθήκη περιεχομένου χωρίς διαγραφή του παλιού (append mode)
with open("example.txt", "a") as file:
    file.write("\nΝέα γραμμή!")

# Ανάγνωση γραμμή-γραμμή
with open("example.txt", "r") as file:
    for line in file:
        print(line.strip())
```

Η εντολή `with` φροντίζει να κλείσει αυτόματα το αρχείο μόλις τελειώσει το μπλοκ κώδικα — δεν χρειάζεται να το κάνεις εσύ χειροκίνητα.

---

## 🔹 Βασικές Εντολές που Αξίζει να Θυμάσαι

```python
input("Δώσε κάτι: ")   # Λήψη εισόδου από τον χρήστη (πάντα ως string)
len([1, 2, 3])            # Μήκος λίστας/string/κτλ.
type(5)                     # Τύπος μιας τιμής
range(5)                     # Ακολουθία αριθμών 0-4
help(print)                   # Εμφανίζει βοήθεια για μια συνάρτηση
```

---

## 🎯 Επόμενα Βήματα

🔹 **Μάθε περισσότερα!** Δες την [επίσημη τεκμηρίωση της Python](https://docs.python.org/3/).

🔹 **Πρακτική!** Δοκίμασε να γράψεις δικά σου προγράμματα — μικρά scripts, calculators, games.

🔹 **Προχώρα στις Δομές Δεδομένων** - Λίστες, λεξικά, πλειάδες και σύνολα σε βάθος.

🔹 **Μάθε OOP** - Οργάνωσε τον κώδικά σου με κλάσεις και αντικείμενα.

🔹 **Εξερεύνησε βιβλιοθήκες!** Pandas, NumPy, Matplotlib για ανάλυση δεδομένων, Flask/Django για web development.


Καλή προγραμματιστική πορεία! 🚀
